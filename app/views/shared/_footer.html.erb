<footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
    <p class="col-md-4 mb-0 text-muted">Besty Esty Shop</p>

    <a href="/" class="col-md-4 d-flex align-items-center justify-content-center mb-3 mb-md-0 me-md-auto link-dark text-decoration-none">
      <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"/></svg>
    </a>

    <ul class="nav col-md-4 justify-content-end">
      <li class="nav-item"><a href="#" class="nav-link px-2 text-muted"><h6>Github Repository <%= link_to session[:repo], 'https://github.com/TannerDale/little-esty-shop' %></h6></a></li>
      <!-- Below could use a definite refactor; couldn't get it working with an if/else statement but that's probably a better idea -->
      <li class="nav-item"><a href="#" class="nav-link px-2 text-muted"> <%= session[:user_names][0] + ", " + session[:user_names][1] + ", " + session[:user_names][2] + ", and " + session[:user_names][3] %> collaborated on this project, making <%= session[:commits] %> commits and generating <%= session[:pulls_count] %> pull requests.</a></li>
    </ul>
  </footer>
